# Fallback pour un hébergement sans worker persistant.
# À adapter au chemin réel du projet et de l'environnement Python.
* * * * * cd /srv/makolo && DJANGO_ENV=production /srv/makolo/.venv/bin/python manage.py run_autopilot >> /var/log/makolo-autopilot.log 2>&1
