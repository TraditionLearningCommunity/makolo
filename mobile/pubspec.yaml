name: makolo_mobile
description: Makolo native mobile client foundation.
publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
